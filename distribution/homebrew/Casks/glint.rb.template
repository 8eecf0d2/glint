cask "glint" do
  version "__VERSION__"
  sha256 "__SHA256__"

  url "__URL__"
  name "Glint"
  desc "Native macOS window manager with Spectacle 1.2 movement behavior"
  homepage "https://glint.broderickwilkinson.com"

__ARCH_REQUIREMENT__
  depends_on macos: ">= :sonoma"

  app "Glint.app"

  uninstall quit: "dev.8eecf0d2.glint"

  zap trash: [
    "~/Library/Preferences/dev.8eecf0d2.glint.plist",
  ]

  caveats <<~EOS
    Glint is ad-hoc signed and not notarized. macOS may require explicit approval
    in System Settings → Privacy & Security, followed by Accessibility access.
    This cask does not remove quarantine or disable Gatekeeper.
  EOS
end
